all:
  children:
    webservers:
      hosts:
        localhost:
          ansible_host: localhost
          ansible_connection: local
    dbservers:
      hosts:
        localhost:
          ansible_host: localhost
          ansible_connection: local